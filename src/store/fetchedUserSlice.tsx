import { createSlice } from "@reduxjs/toolkit";

const fetchedUserSlice = createSlice({
  name: "fetchedUser",
  initialState,
  reducers: {},
});

export const {} = fetchedUserSlice.actions;

export default fetchedUserSlice.reducer;
